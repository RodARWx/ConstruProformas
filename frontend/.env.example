# URL base del API de Construproformas.
# Desarrollo local (con proxy Vite): /api
# Producción (dominio o IP del backend; el build normaliza el protocolo y sufijo /api):
#   http://192.168.1.100:3000/api
VITE_API_BASE_URL=/api
